## 2024-05-18 - QLineEdit setClearButtonEnabled
**Learning:** Added clear button to QLineEdits for a quick reset option. The PySide6 API supports this feature natively via setClearButtonEnabled(True), avoiding custom icons and event handlers.
**Action:** Use native widget features in PySide6 to add common UI affordances like clear buttons for inputs when possible.
